import {LoginInputModel} from "../models/auth/input";

export class AuthRepository {
    static async addUser() {
        const newUser = {

        }
        // await userCollection.insertOne({loginOrEmail: string, hash});



    }
}
