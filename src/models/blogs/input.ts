export type BlogInputModel = {
    name: string;
    description: string;
    websiteUrl: string;
}

export type BlogUpdateModel = {
    name: string;
    description: string;
    websiteUrl: string;
}
