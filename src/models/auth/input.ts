export type LoginInputModel = {
    loginOrEmail: string;
    password: string;
}

export type LoginSessionData = {
    ip: string;
    title: string;
}

export type RegisterInputModel = {
    login: string;
    email: string;
    password: string;
}
